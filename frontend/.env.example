VITE_API_TARGET=http://localhost:3001

# Optional: only use this if you want the browser to call the backend directly.
# Production on Vercel uses same-origin /api and /health rewrites instead.
# VITE_USE_DIRECT_API=true
# VITE_API_URL=https://watchdog-to6v.onrender.com
